{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": "14.x",
    "npm": "6.14.8"
  },
  "scripts": {
    "test": "echo 'Error: no test specified' && exit 1",
    "server": "node-env-run index.js --exec nodemon",
    "start": "node-env-run index.js --exec nodemon",
    "install-client": "cd client && npm install",
    "client": "cd client && npm start",
    "dev": "run-p server client",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "mongoose": "^5.12.3",
    "multer": "^1.4.2",
    "notenv": "^2.0.3",
    "node-env-run": "^4.0.2",
    "nodemon": "^2.0.7"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
